# План нагрузочного и стресс-тестирования

Тестирование строится вокруг ключевых бизнес-транзакций, а не вокруг полного перебора всех endpoints. Auth-запросы включены в цепочки как подготовительный шаг, потому что основные операции требуют access token.

## Сценарии

| Сценарий | Количество запросов в 1 итерации | Плановая интенсивность на плато | Общее число итераций за 20 минут | Общее число HTTP-запросов | Цепочка вызовов |
|---|---:|---:|---:|---:|---|
| Создание комнаты | 3 | 4 итерации/мин, около 0.2 RPS | около 60 | около 180 | `POST /rooms` -> `GET /rooms/{roomId}` -> `POST /rooms/{roomId}/end` |
| Подключение к комнате | 6 | 65 итераций/мин, около 6.5 RPS | около 975 | около 5850 | `POST /rooms` -> `POST /invites/{inviteToken}/join` -> `GET /rooms/{roomId}` -> `POST /rooms/{roomId}/media-token` -> `POST /rooms/{roomId}/leave` -> `POST /rooms/{roomId}/end` |
| Чат в комнате | 4 | 45 итераций/мин, около 3.0 RPS | около 675 | около 2700 | `POST /rooms` -> `POST /rooms/{roomId}/messages` -> `GET /rooms/{roomId}/messages` -> `POST /rooms/{roomId}/end` |

## Методика

Плановая нагрузка:

- инструмент: k6;
- файл: `tests/load/load_core.js`;
- длительность: 20 минут;
- ramp-up: 5 минут;
- plateau: 10 минут;
- ramp-down: 5 минут;
- тип нагрузки: `ramping-arrival-rate`, то есть управление числом транзакций в минуту, а не свободным количеством VU;
- суммарный пик на плато: около 9.7 RPS;
- ожидаемый объем: около 8730 HTTP-запросов за полный прогон;
- допустимый error rate: меньше 10%;
- целевой p95 HTTP API: меньше 3000 мс.

Стресс-тест:

- файл: `tests/load/stress_core.js`;
- стартует с плановой нагрузки;
- за 5 минут выходит на плановый уровень;
- затем увеличивает arrival rate на 10% каждые 10 секунд;
- базовый прогон растет 60 шагов, то есть 10 минут роста;
- ориентировочный пик базового stress: около 68 RPS;
- точка деградации фиксируется при error rate больше 25%;
- превышение 25% в стресс-тесте не считается ошибкой методики, а является целевым результатом поиска breaking point.

Усиленный stress-прогон:

- файл: `tests/load/stress_breakpoint.js`;
- используется, если базовый stress-прогон не достиг error rate 25%;
- стартовые arrival rates выше базовых;
- рост продолжается 120 шагов по 10 секунд;
- ориентировочный пик: около 170 RPS.

Нагрузка x10:

- файл: `tests/load/x10_core.js`;
- использует те же сценарии, но с повышенной arrival-rate нагрузкой;
- ориентир выбран по примеру отчета: пиковая суммарная интенсивность около 60 RPS;
- ожидаемый объем: около 54 000 HTTP-запросов за 20 минут;
- результат фиксируется как выдержала/не выдержала, с указанием p95, error rate и предполагаемого узкого места;
- для x10 допускается более мягкий технический порог: error rate меньше 25% и p95 меньше 5000 мс.

## Таблица x10 для отчета

| Сценарий | Количество запросов в 1 итерации | Интенсивность x10 на плато | Общее число итераций за 20 минут | Общее число HTTP-запросов |
|---|---:|---:|---:|---:|
| x10_create | 3 | 40 итераций/мин, около 2 RPS | около 600 | около 1800 |
| x10_join | 6 | 450 итераций/мин, около 45 RPS | около 6750 | около 40500 |
| x10_chat | 4 | 195 итераций/мин, около 13 RPS | около 2925 | около 11700 |
| Итого | - | около 60 RPS | около 10275 | около 54000 |

## Таблица stress для отчета

| Сценарий | Базовая интенсивность | Алгоритм роста | Ориентировочный пик базового stress | Критерий деградации |
|---|---:|---|---:|---|
| Создание комнаты | 4 итерации/мин | +10% каждые 10 секунд после 5 минут ramp-up | около 28 итераций/мин | error rate > 25% |
| Подключение к комнате | 65 итераций/мин | +10% каждые 10 секунд после 5 минут ramp-up | около 455 итераций/мин | error rate > 25% |
| Чат в комнате | 45 итераций/мин | +10% каждые 10 секунд после 5 минут ramp-up | около 315 итераций/мин | error rate > 25% |

## Команды

```bash
env GOCACHE=/tmp/radiance-go-cache go test ./...
docker compose --env-file deployments/.env -f deployments/docker-compose.yml up --build
curl -fsS http://localhost:8080/health/ready
k6 run tests/load/http_smoke.js
k6 run tests/load/load_core.js
k6 run tests/load/stress_core.js
k6 run tests/load/stress_breakpoint.js
k6 run tests/load/x10_core.js
```

Для запуска против другого адреса:

```bash
BASE_URL=http://localhost:8080 k6 run tests/load/load_core.js
```

Для отделения прогонов друг от друга можно задать `TEST_RUN_ID`:

```bash
TEST_RUN_ID=report-01 BASE_URL=http://localhost:8080 k6 run tests/load/load_core.js
```

Сценарии переиспользуют load-test пользователей внутри VU и закрывают созданные комнаты через `/end` или `/leave` + `/end`. Для очистки данных, накопленных старыми прогонами, можно выполнить:

```bash
docker compose --env-file deployments/.env -f deployments/docker-compose.yml exec -T postgres psql -U radiance -d radiance < tests/load/cleanup_load_data.sql
```
